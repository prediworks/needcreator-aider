/** Scrolls the page down once (used for lists such as hashtag pages or the ad library) */
(() => { window.scrollBy(0, Math.max(600, window.innerHeight * 0.9)); return document.documentElement.scrollHeight; })();
